import { TestoCustomPipe } from './testo-custom-pipe';

describe('TestoCustomPipe', () => {
  it('create an instance', () => {
    const pipe = new TestoCustomPipe();
    expect(pipe).toBeTruthy();
  });
});
